# Lesson 0A.1 — The Relational Model: Tables, Rows, Columns

Starter queries for **[Lesson 0A.1 (script)](https://abbottics-ai.atlassian.net/wiki/spaces/DTA/pages/125960194)**.

- **Database:** AdventureWorksLT (`SalesLT` schema)
- **Query budget:** 5 — all orientation; **no filtering yet**

## What you'll do
- Read a result set as **rows** (records) and **columns** (attributes).
- Tour the core SalesLT entities: `Customer`, `Product`, `SalesOrderHeader`.
- Count the rows in a table.
- See that result order isn't guaranteed unless you ask for it.

## Run it
Open [`01-explore-tables.sql`](01-explore-tables.sql), connect with the pre-seeded **AIDD-MS Lab (AdventureWorksLT)** profile, and run each statement with the ▶ button.
